


    // 1. 사용할 module을 가져와서 변수에 저장
    var hahaha = require("./circle");

    console.log(hahaha.area(5));
    console.log(hahaha.sample);
    // 1-1. 현재폴더 밑에서 circle이라는 폴더를 찾고
    // 그 안에 index.js를 찾아요
    // 1-2. 현재폴더 밑에서 circle.js를 찾아요